Cozy Cafe Core

React — UI component library
Vite — build tool and dev server
React Router DOM — client-side page routing
JavaScript (JSX) — component logic

Styling

CSS — custom styling for every component
CSS Variables — consistent color palette across the site

Testing & Documentation

Storybook — component isolation and documentation
Chromatic — visual regression testing

Version Control

Git — version control and commit history

Dev Environment

VS Code — code editor
Node.js / npm — package management
